import TestShell from "@/components/TestShell";
import FormPerceptionApp from "@/components/FormPerceptionApp";

export default function FormPerceptionPage() {
  return (
    <TestShell title="หารูปที่เหมือนกัน">
      <FormPerceptionApp />
    </TestShell>
  );
}
